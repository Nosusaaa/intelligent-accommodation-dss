# Point the SPA at a shared team API (omit or leave empty to use same-origin /api via Vite proxy).
# Example when one machine runs FastAPI for everyone (replace host with LAN IP):
# VITE_API_BASE_URL=http://192.168.1.10:8000/api
#
# After changing, restart `npm run dev`.
VITE_API_BASE_URL=
